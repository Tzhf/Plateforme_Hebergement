<ul class="navbar-nav bg-gradient-primary sidebar text-center sidebar-dark accordion" id="accordionSidebar">
  <div class="sticky-top">
    <!-- Sidebar - Brand -->
      <a class="sidebar-brand d-flex align-items-center justify-content-center" href="{{ path('logements_show') }}">
        <div class="sidebar-brand-icon rotate-n-15">
          <i class="fas fa-bed"></i>
        </div>
        <div class="sidebar-brand-text mx-3">PLATEFORME HEBERGEMENT</div>
      </a>

      <!-- Divider -->
      <hr class="sidebar-divider my-0">

      {% if app.user %}
        
        {% if app.user.roles[0] == 'ROLE_ADMIN' %}
        <!-- Nav Item - Admin Dashboard -->
          <li class="nav-item">
            <a href="{{ path('admin_gestionnaires_show') }}" class="nav-link">
              <i class="fas fa-fw fa-tachometer-alt"></i>
              <span>Administration<span>
            </a>
          </li>

          <!-- Divider -->
          <hr class="sidebar-divider my-0">
        {% endif %}

        <li class="nav-item">
          <a href="{{ path('logements_show') }}" class="nav-link">
            <i class="fas fa-fw fa-bed"></i>
            <span>Mes logements</span>
          </a>
        </li>

        <!-- Divider -->
        <hr class="sidebar-divider">

        <!-- Heading -->
        <div class="sidebar-heading">
          MON COMPTE
        </div>

        <!-- Nav Item - Options -->
        <li class="nav-item">
          <a href="{{ path('options', {'id': app.user.id, 'token': csrf_token('options-' ~ app.user.id) }) }}" class="nav-link">
            <i class="fas fa-fw fa-cog"></i>
            <span>Options</span>
          </a>
        </li>


        <!-- Nav Item - Déconnexion -->
        <li class="nav-item">
          <a class="nav-link" href="{{ path('deconnexion') }}">
            <i class="fas fa-fw fa-user-slash"></i>
            <span>Déconnexion</span>
          </a>
        </li>

      {% else %}
        <!-- Nav Item - Connexion -->
        <li class="nav-item">
          <a href="{{ path('connexion') }}" class="nav-link">
            <i class="fas fa-fw fa-user"></i>
            <span>Se connecter</span>
          </a>
        </li>

        <!-- Nav Item - Inscription -->
        <li class="nav-item">
          <a href="{{ path('inscription') }}" class="nav-link">
            <i class="fas fa-fw fa-user-plus"></i>
            <span>Inscription</span>
          </a>
        </li>
      {% endif %}

      <!-- Divider -->
      <hr class="sidebar-divider d-none d-md-block">

      <!-- Sidebar Toggler (Sidebar) -->
      <div class="text-center d-none d-md-inline">
        <button class="rounded-circle border-0" id="sidebarToggle"></button>
      </div>
  </div>
</ul>